<UserControl x:Class="WPF_MVVM_SPA_Template.Views.ClientFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MVVM_SPA_Template.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="AliceBlue" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Content="ID" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBlock Text="{Binding SelectedClient.ID}" Grid.Row="0" Grid.Column="1" Margin="5"/>
        
        <Label Content="Nom" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
        <TextBox Text="{Binding SelectedClient.Nom, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Label Content="Cognoms" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
        <TextBox Text="{Binding SelectedClient.Cognoms, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="2" Grid.Column="2" Margin="5"/>

        
        <Label Content="Email" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" />
        <TextBox Text="{Binding SelectedClient.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="3" Grid.Column="3" Margin="5"/>

        
        <Label Content="Telèfon" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" />
        <TextBox Text="{Binding SelectedClient.Telefon, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="4" Grid.Column="1" Margin="5"/>

       
        <Label Content="Data d'Alta" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" />
        <DatePicker SelectedDate="{Binding SelectedClient.DataAlta, Mode=TwoWay}" 
                    Grid.Row="5" Grid.Column="1" Margin="5" />

        <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="1" HorizontalAlignment="Right">
            <Button Content="Guardar" Command="{Binding SaveCommand}" Margin="5" Width="100" Height="50" />
            <Button Content="Cancel·lar" Command="{Binding CancelCommand}" Margin="20" Width="100" Height="50"/>
        </StackPanel>

    </Grid>
</UserControl>
