<UserControl x:Class="WPF_MVVM_SPA_Template.Views.Option1View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_MVVM_SPA_Template.Views"
        mc:Ignorable="d">
    <Grid Background="White" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Students}" 
                  SelectedItem="{Binding SelectedStudent}" 
                  IsReadOnly="True"
                  AutoGenerateColumns="False" 
                  Grid.Row="0">
            
        </DataGrid>
        <Label Content="Welcome to our Bank Management App!" HorizontalAlignment="Left" Margin="25" VerticalAlignment="Top" Width="Auto"/>

    </Grid>
</UserControl>
